import { Meta } from "@storybook/addon-docs/blocks";
import { border, zIndex } from "../tokens/tokens";

<Meta title="Foundations/Other" />

# Other

Прочие системные токены.

## Правила

- Использовать только по назначению
- Не смешивать с основными token groups
- При частом использовании переносить в отдельную группу

## Border width

<div style={{ display: "grid", gap: 8, marginBottom: 32 }}>
  {border.map((token) => (
    <div
      key={token.name}
      style={{
        display: "grid",
        gridTemplateColumns: "220px 1fr",
        gap: 16,
        paddingBottom: 8,
        borderBottom: "1px solid var(--color-border-subtle)"
      }}
    >
      <div style={{ fontFamily: "ui-monospace, monospace", fontSize: 12, color: "var(--color-text-secondary)" }}>
        {token.name}
      </div>
      <div style={{ color: "var(--color-text-primary)", fontSize: 13 }}>{token.value}</div>
    </div>
  ))}
</div>

## Z-index

{zIndex.length === 0 && (
  <p style={{ color: "var(--color-text-secondary)" }}>
    В Figma `01_FOUNDATIONS` z-index-слои пока не определены. Раздел будет заполнен, когда появятся соответствующие токены.
  </p>
)}

{zIndex.map((token) => (
  <div
    key={token.name}
    style={{
      display: "grid",
      gridTemplateColumns: "220px 1fr",
      gap: 16,
      paddingBottom: 8,
      borderBottom: "1px solid var(--color-border-subtle)"
    }}
  >
    <div style={{ fontFamily: "ui-monospace, monospace", fontSize: 12, color: "var(--color-text-secondary)" }}>
      {token.name}
    </div>
    <div style={{ color: "var(--color-text-primary)", fontSize: 13 }}>{token.value}</div>
  </div>
))}
